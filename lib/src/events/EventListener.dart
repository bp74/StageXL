part of dartflash;

typedef void EventListener(event);

// ToDo: why does the typedef below not work in checked mode ?
// throws an error for "onMouseEvent(MouseEvent me)

//typedef void EventListener(Event event);
